// import nodemailer from "nodemailer";

// const email = process.env.NEXT_PUBLIC_ID; // 메일 주소
// const pw = process.env.NEXT_PUBLIC_SECRET; //비밀번호 선언

// export const transporter = nodemailer.createTransport({
//   name: email,
//   pw : pw,
// });

// export const mailOptions = {
//   from: email, // 송신 이메일
//   to: email, // 수신 이메일
// };
